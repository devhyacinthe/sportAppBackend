{
  "version": 2,
  "builds": [{ "src": "src/server.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "src/server.ts" }],
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"back\" ] && exit 1 || exit 0"
}
